<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tower.game.mapper.PlayerMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.tower.game.model.entity.Player">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="level" property="level"/>
        <result column="exp" property="exp"/>
        <result column="current_floor" property="currentFloor"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 查询高等级玩家 -->
    <select id="findHighLevelPlayers" resultMap="BaseResultMap">
        SELECT * FROM player
        WHERE level >= #{minLevel}
        ORDER BY level DESC, exp DESC
        LIMIT 100
    </select>

</mapper>
